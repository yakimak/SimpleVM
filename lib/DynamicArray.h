#ifndef DYNAMIC_ARRAY_FORWARD_H
#define DYNAMIC_ARRAY_FORWARD_H

// Forwarder header for tooling / include-path quirks.
// The real implementation lives in `lib/LazySequence/DynamicArray.h`.
#include "LazySequence/DynamicArray.h"

#endif // DYNAMIC_ARRAY_FORWARD_H

